-- Эта миграция удаляет устаревшие таблицы, связанные со старой,
-- более не используемой системой аутентификации.

-- Удаляем таблицу с глобальными настройками приложения
DROP TABLE IF EXISTS public.app_settings;

-- Удаляем таблицу с билетами сессий
DROP TABLE IF EXISTS public.session_tickets;
